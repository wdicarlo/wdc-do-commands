Commands to evaluate:
    multistrap
    ss
    mount --bind
    httpie
    lumail
    pwstore (in sqlite db)
    watch
    bash-it
    fasd
    direnv
    socli
    cats
    has (https://github.com/kdabir/has)
    watchdog (https://github.com/gorakhargosh/watchdog.git)
    github.com/micha/json-table
Evaluate command history by folder
Add todo management
Develop Lua simple type checking
Evaluate tools for backup: irsync, rsnapshot, rbackup, 
Add command to join output from different other commands (git blame, git diff, git log)
Solve bug with 'grep "$num"' by adding a space after num
https://github.com/0k/shyaml for YAML from command line
Extract aknowledgements with command d fs "\"#.*http[s]*://\""
Store executed commands in project specific history file

